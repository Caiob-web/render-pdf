{
  "name": "render-pdf",
  "version": "1.0.0",
  "private": true,
  "description": "Projeto Vercel para gerar PDFs simples e lote (HTML -> PDF) via Puppeteer",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "test": "echo \"No tests\" && exit 0"
  },
  "dependencies": {
    "@sparticuz/chromium": "*",
    "puppeteer-core": "*",
    "jszip": "^3.10.1",
    "pdf-lib": "^1.17.1"
  }
}
